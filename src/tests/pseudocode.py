# Capture image from OpenCV
# Find all contours in the image
for contour in contours:
	# Approximate a polygon for the contour
	# Count the number of distinct edges
	if len(edges) > 15:
		# Circle has been detected
	elif len(edges) == 4:
		# Triangle has been detected
	elif len(edges) == 4:
		# Rectangle has been detected
	elif len(edges) == 5:
		# Pentagon has been detected